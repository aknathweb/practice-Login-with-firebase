import React from 'react';

const Thirdparty = () => {
    return (
        <div>
            <button>Google</button>
            <button>GitHub</button>
        </div>
    );
};

export default Thirdparty;